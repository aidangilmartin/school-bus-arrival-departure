[build]
  publish = "public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Serve the static board and functions without running the Node server.
[dev]
  framework = "#static"
