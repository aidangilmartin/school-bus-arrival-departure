// Public runtime config. The Google OAuth *client ID* is not a secret — it is
// meant to be embedded in the frontend. Replace the value below with the Web
// client ID from Google Cloud Console. When empty, staff sign-in is hidden and
// the site shows the read-only student view only.
window.APP_CONFIG = {
  googleClientId: "",
};
